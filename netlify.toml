[build]
  # Este proyecto genera los assets estáticos en `dist/`.
  publish = "dist"

  # Forzamos pnpm (Netlify usa Corepack). Evita que el deploy quede desincronizado.
  command = "corepack enable && pnpm run build"