import { defineNuxtUIConfig } from '@nuxt/ui';

export default defineNuxtUIConfig({
  theme: {
    colors: {
      primary: '#006341', // Verde institucional
      accent: '#B6D7B9', // Verde acento suave
      dark: '#004D32',   // Verde oscuro
    }
  }
}); 